Place sample CSVs here for local testing.

Required:
- credit.csv with columns:
  txn_id, customer_id, txn_date, sku, region, amount, price, quantity
- panel.csv with columns:
  panel_id, customer_id, age_group, region, income_bin, household_size

Optional:
- demographics.csv (unused in MVP)

Example minimal rows are shown in repository README.
